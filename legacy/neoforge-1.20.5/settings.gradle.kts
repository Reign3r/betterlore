pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        maven("https://maven.neoforged.net/releases/")
    }
}

rootProject.name = "better-lore-neoforge-1.20.5-legacy-userdev"
